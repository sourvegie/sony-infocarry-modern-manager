# P17-008 — independent R3 review

Date: 2026-09-01
Reviewed baseline: canonical `main` after PR #14, `a79ceca`
Reviewed task branch: `task/P17-008-verified-raw-backup-state-identity`
Disposition: **PASS — READY_FOR_HARDWARE_TEST at the host-only boundary**

## Review scope

This record is reserved for the independent R3 review of the P17-008 raw
backup-state identity correction. The reviewer must inspect the implementation,
focused tests, P17-005 regression behavior, the P17-007 sanitized fail-closed
record, and the external evidence preservation statement without modifying raw
evidence, accessing hardware, or invoking a sender.

Required review questions:

- Is identity derivation strictly downstream of complete/integrity backup
  verification?
- Does equality fail on every material device, protocol, object-set, role,
  command, filename-semantic, length, raw-hash, dynamic, and fixed-state
  difference?
- Are archive path, manifest hash, and acquisition timestamps excluded only
  from state equality while remaining preserved and explicitly reported?
- Are candidate bytes, transaction bytes, and all non-provenance operation
  bindings still exact before sender construction?
- Are one-shot, cancellation, completion, no-retry, post-backup, read-back, and
  GUI/CLI isolation controls unchanged?
- Do focused tests provide meaningful coverage without treating fake transport
  as physical evidence?

## Initial independent review findings

The initial independent review found no material state-identity or sender
safety bypass. It placed the branch on hold for two correctable items:

1. The first post-change full-suite observation found one test expectation
   failure: a synthetic archive path change does not necessarily change a
   manifest hash when the manifest bytes are otherwise identical.
2. The focused identity tests did not yet exercise protocol extras,
   requested-length drift, dynamic-blob differences, fixed-state differences,
   or per-object acquisition timestamps independently.

## Corrections

The path-only assertion now checks the guaranteed `archive_directory`
provenance difference. A separate recapture test changes capture timestamps
and verifies the manifest-hash provenance difference. Focused tests now cover
protocol extras, requested-length drift, dynamic blob changes, fixed-state
changes, and per-object `received_at_utc` provenance. The adapter remains
unchanged with respect to sender count, no-retry, completion, cancellation,
post-backup, read-back, and normal GUI/CLI isolation.

After the correction, focused identity/adapter validation passes (14 tests),
and the complete portable suite passes (**596 tests, 3 intentional skips**).
`git diff --check` also passes. The final independent re-review and CI result
must be recorded here before the branch is reported as
`READY_FOR_HARDWARE_TEST`.

## Final independent R3 re-review

The correction was independently re-reviewed against the current working tree.
The reviewer found no material implementation or safety findings and confirmed:

- complete/integrity verification precedes raw-state identity derivation;
- device, protocol, canonical object order/roles/commands, filename semantics,
  requested/received lengths, raw hashes, dynamic blob, and fixed-state hashes
  are all equality-bound;
- archive path, manifest hash, and acquisition/process timestamps remain
  preserved and separately reported as provenance;
- exact candidate bytes, exact transaction bytes, and all non-provenance
  bindings remain required before sender construction;
- one-shot, no-retry, completion, post-backup/read-back, and GUI/CLI isolation
  controls remain intact; and
- the added focused mutations meaningfully cover provenance-only equality and
  material state changes.

Final host validation is **596 passing tests, 3 intentional skips**, with
`git diff --check` passing. The re-review did not access hardware, USB, sender,
or external evidence. P17-008 is **READY_FOR_HARDWARE_TEST** at the host-only
boundary and must stop; a later task needs a new fresh preflight and new
operation-specific approval.
