InfoCarry Manager — macOS packaged runtime notices

The application source is licensed under the MIT License. See LICENSE.

Python runtime
  CPython is provided by the selected build interpreter and bundled by
  PyInstaller. The Python runtime is PSF-licensed. The selected Python
  distribution's license and notices remain with the bundled runtime.

Tcl/Tk runtime
  Tcl/Tk 9.0 or newer is required. The runtime files are collected from the
  selected Python distribution and remain in the onedir application bundle.
  Tcl/Tk license notices are included with that runtime.

PyUSB 1.3.1
  BSD-3-Clause. https://github.com/pyusb/pyusb

libusb-package 1.0.30.0
  Python package code: Apache-2.0.
  Bundled libusb 1.0 shared library: LGPL-2.1-or-later.
  https://github.com/pyocd/libusb-package
  https://github.com/libusb/libusb
  The dylib remains a separate replaceable file in the onedir application.

PyInstaller 6.22.3
  GPL-2.0-or-later with the PyInstaller bootloader exception.
  https://github.com/pyinstaller/pyinstaller

The bundle includes installed package metadata and license resources where
those distributions provide them. This notice is a locator and summary, not
a replacement for the corresponding license texts.
