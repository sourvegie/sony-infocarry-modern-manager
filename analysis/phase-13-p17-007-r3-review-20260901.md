# P17-007 — independent R3 review

Date: 2026-09-01
Baseline: canonical `main` at `ca57b33` (PR #13)
Reviewed branch: `task/P17-007-exact-library-package-live-smoke`
Disposition: **ESCALATION_REQUIRED — fail-closed before transmission**

## Review scope

The independent review examined the P17-007 sanitized result, the merged
P17-005 execution boundary, the external evidence root

`/Users/stardust/Projects/InfoCarry-Evidence/phase-17-p17-007-library-package-live-smoke-20260901-01/`

and the preservation manifest. The review did not edit files, issue USB
requests, construct a sender, request or reinterpret approval, or retry the
operation.

## Findings

- The fresh native `0x0019` responses are byte-identical and hash to
  `c33328b686dee7fdc005731a5ded428d76415e91ced03edad63646063394662`; parsed
  offset `+0x08` is `3,145,728` bytes.
- The three P17-007 backup archives reviewed for the preflight and execution
  attempt are complete, contain eight length-valid objects, and pass
  independent hash verification. Their raw objects, including the
  2,075,256-byte dynamic blob, are byte-identical across the relevant
  captures. The sealed preflight backup manifest SHA is
  `46688511f03e9f5ebf7e25bef0017a64a3ebc92482c10b535ecd70fc75ef92fe`; the
  additional fresh pre-write manifest SHA is
  `35a203ba8df6c351b473c18b364c52b1e507973f6e27f49fd5bfbf56f18a2636`.
- The target was absent from the fresh backup state. The exact candidate and
  transaction remained equal to the approved P17-006 values:
  `0e3af665c6046c91d1096e8570f8b63b1fc82d35788027becf65ff942ca8883f` and
  `1abab51a9ddb069154fba4d411ffbe31359125270b1438d78acd47b745a54439`.
- P17-005 rejected the additional backup at
  `preflight_revalidation` because its full archive identity differed from
  the sealed baseline. The comparison identifies changed capture-generated
  archive/object timestamps while raw object hashes and device identity stay
  equal. No byte normalization was performed.
- The failure occurred before sender construction and backend access:
  `sender_calls=0`, `backend_method_calls=0`, `write_backend_opened=false`,
  `write_started=false`, and USB transmission was false. The failure audit is
  preserved at `04-audit/execution-failure-0001.json`.
- The external version-2 preservation manifest covers 73 files and replays
  with zero hash/size mismatches. Raw candidate and transaction bytes remain
  outside Git.

## R3 decision

The correct safety disposition is **ESCALATION_REQUIRED**. The external
evidence is complete; the unresolved issue is the safe identity boundary for
the additional fresh pre-write backup. P17-005 currently binds the full
archive identity, including generated timestamps, so the approved phrases
cannot safely be reinterpreted to bypass its rejection. Do not retry,
normalize the backup, request a new write, or transmit until a Project Lead
decision and separately reviewed host correction resolve that policy.

The candidate’s physical compatibility, completion value, and post-operation
state remain unverified because no transaction occurred. P17-007 therefore
does not reach `COMPLETE` or `READY_FOR_HARDWARE_TEST`.
