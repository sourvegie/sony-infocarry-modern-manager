# P17-019 — Independent R3 review

Date: 2026-09-01
Review basis: P17-019 working tree after finite correction round
Risk: R3 — post-operation verification and audit boundary
Disposition: **PASS**

## Scope

This review covers the offline correction for the P17-018 surrounding
wrapper false-negative. No hardware, sender, approval phrase, or `0x101b`
activity is included in this review.

## Findings and correction

The initial review found one P2 backup-binding gap: the wrapper type-checked
the runner backups and compared their paths but did not compare the runner
before-backup raw state with the sealed preflight/candidate baseline or
require distinct before/after evidence directories. The finite correction
round added canonical `BackupStateIdentity` comparisons against both sealed
baselines, requires distinct before/after directories, and adds raw
before-backup substitution regression coverage. The original finding is
resolved.

## Independent checks

- The wrapper accepts only a successful sealed
  `PreparedLibraryPackageLiveResult`.
- It invokes the second disk-only verifier with
  `result.preflight.candidate.core`, never the enclosing preflight object.
- It requires verified, distinct before/after backups and binds the runner
  before state to the sealed preflight and candidate baseline.
- It requires one logical sender call, explicit integer `0x0000`, and
  `automatic_retry_allowed=false`; low-level bulk writes remain separate
  accounting and cannot be reported as sender calls.
- Candidate substitution, raw backup substitution, shared before/after
  evidence, post-backup mismatch, nonzero/missing completion, multiple
  logical sends, and verifier exceptions remain terminal failures.
- The historical P17-018 `live-failure-audit.json` remains outside Git and
  unchanged.

## Validation

- Focused P17-019 adapter suite: **40 passed**.
- Complete portable suite: **629 passed, 3 intentional skips**.
- Python 3.12 compilation: passed.
- `git diff --check`: passed.
- Trailing-whitespace scan of changed files: passed.
- Excluded-evidence/history audit: no raw backup, USB capture, transaction,
  candidate, or P17-018 attempt artifact in tracked files or reachable Git
  history.
- Preserved P17-018 manifest replay: 29 entries, zero mismatches, zero
  unlisted evidence files; manifest SHA-256
  `31816bac14a7b1d0152e10abec833dac094a9447415cafd8397cc6c834958145`.

## Final review conclusion

**PASS.** The P17-019 correction is host/offline-only, preserves the
production runner and all device-safety gates, and resolves the reviewed
wrapper false-negative without converting genuine verification failures into
success. No hardware operation or approval boundary is authorized by this
task.
