# P16-002 — independent R3 review

Date: 2026-08-30
Risk: **R3 — device/safety critical**
Review boundary: P16-002 host preparation only; no device access or modern
write was authorized or performed.

## Review scope

This is the separate implementation/review pass for the P16-002 readiness
boundary. The review covered the complete P16-002 dossier, the sanitized
fixture and hash manifest, the P16-001 Capture 01 results and R3 review, the
latest preserved P16-001 post-operation backup binding, the candidate builder,
authorization gate, independent verifier, isolated runner, and focused test
changes. The P16-001 `_01` evidence and the external raw evidence roots were
not changed.

## Required checks

| Check | Result | Classification |
| --- | --- | --- |
| Canonical starting point | `main` at merge `915ac5c`; task branch created from clean current main | verified |
| Exact package shape | One new root folder, three direct children, TXT → BMP → TXT; `_02` distinct from native `_01` | verified |
| Source fixture | 144-byte TXT, 10,302-byte validated BMP, 139-byte TXT; `SHA256SUMS.txt` passes | verified |
| Native reconciliation | P16-001 folder/marker geometry, ordered child records, 32-byte TXT prefixes, 16-byte all-`FF` BMP prefix, payloads, alignment, fixed-state objects, and preservation findings are bound | observed native facts; verified offline reuse |
| Baseline | Latest complete P16-001 post-operation backup and dynamic-blob hashes are used; stale pre-Capture-01 backup is not used | verified |
| Candidate | Checksum-valid 394-record candidate; exact offsets, payload lengths, prefixes, allocation, timestamp policy, and unrelated-byte preservation are audited | verified offline |
| Authorization | Device, baseline, source paths/hashes/order, target paths/kinds/offsets, native template identity/paths/offsets/prefixes, fixed state, capacity reference, candidate, transaction, policy, and expected post delta are bound | verified offline |
| Capacity | Parsed native `0x0019` reference is bound for construction only; future live preflight must query a fresh response | verified offline; live fact unresolved |
| Runner isolation | P16-specific module is absent from the normal CLI/GUI import graph; no USB is opened on import | verified by source inspection |
| One-shot safety | Exact P16 shape is enforced; one sender call maximum; only integer `0x0000`; automatic retry is disabled | verified by code/tests |
| Failure handling | Pre-send cancellation, after-start cancellation, timeout, disconnect, malformed/missing/nonzero completion, post-backup/read-back failure, drift, conflict, tampering, and second-send refusal are covered | verified by injected fake-host tests |
| Evidence preservation | P16-002 derived artifacts are under a new non-overwriting external root; P16-001 evidence is untouched; preservation manifest v4 validates 34 entries | verified |

## Corrections resolved during review

The review pass found and resolved these implementation issues before the
disposition below:

1. The expanded authorization path referenced `Mapping` without importing it.
   The focused gate/live-runner suite caught the defect; the import was added
   and the suite passed.
2. The first audit expansion did not explicitly bind source paths, native
   template identity/prefix details, or the expected post-operation delta.
   The candidate audit, authorization, external supplements, and mutation
   tests were expanded to bind and reject those changes.
3. The isolated sender could be called directly with a generic multi-package
   candidate. It now revalidates the exact P16-002 package shape before its
   one-shot counter is consumed.

No correction changed raw evidence, P16-001 artifacts, the candidate bytes,
the prospective transaction bytes, or the normal GUI/CLI surface.

## Validation record

- Focused P16/P15 package, candidate, gate, verifier, workflow, protocol, and
  CLI tests: **81 passed**.
- P16-002 focused gate/live-runner/fixture tests after corrections: **14 passed**.
- Complete portable suite: **539 passed, 3 intentional skips**.
- CI results are recorded on the final review update after the branch is
  committed and pushed.
- `git diff --check`: passed for the current worktree.
- External evidence exclusion audit: pending final committed-history audit.

## Classification and disposition

### Verified

The exact sanitized fixture, deterministic candidate, source/path/order
bindings, native template reuse, prefix/payload rules, checksum, capacity
arithmetic, fixed-state preservation, one-shot fake boundary, failure handling,
and independent offline verifier are host-verified for the constrained
TXT/BMP/TXT shape.

### Observed

P16-001 supplies the native legacy mixed-package record relationships,
type-specific prefixes, payload persistence, timestamp behavior, normal
Manager outcome, and device accessibility observation. Those observations are
not generalized to arbitrary modern packages.

### Inferred

The P16-001 templates and insertion geometry can construct the exact `_02`
candidate offline while the approved modern policy preserves existing
timestamps and uses one explicit timestamp for new records.

### Unresolved

Fresh live `0x0019` response semantics, native numeric completion decoding,
physical acceptance of the newly constructed modern mixed package, and
interrupted-write recovery remain unresolved. A future live task must resolve
its own fresh preflight facts and receive new operation-specific approval.

### Final disposition

**PASS — READY_FOR_HARDWARE_TEST**, limited to the exact P16-002 host-prepared
flat TXT/BMP/TXT package and the isolated future runner boundary. This review
does not authorize device detection, backup, capacity query, or a modern
`0x101b` transaction. The next task must stop for a new explicit approval
before any device-changing action.
