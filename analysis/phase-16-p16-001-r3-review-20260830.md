# P16-001 — independent R3 host-readiness review

Date: 2026-08-30
Disposition: **PASS — READY_FOR_HARDWARE_TEST; no device operation performed**
Risk: **R3 — device/safety critical**

This is the strong independent host-side review of the P16-001 native
mixed-package capture plan. It confirms a stop point before the legacy
Manager send; it is not owner approval and it does not authorize hardware
access.

## Review checks

| Check | Result |
| --- | --- |
| Task branch starts from clean merged canonical `main` after PR #3 | Pass; `ae35ec2` |
| P15-003 is recorded complete before P16 work | Pass |
| New root folder is distinct from P15 `_01` and `_02` | Pass; `IC_P16_MIXED_20260830_01` |
| Fixture has exactly one folder and ordered TXT/BMP/TXT children | Pass |
| Source sizes, hashes, sentinels, strict CP932 policy, and CRLF are exact | Pass; focused tests |
| BMP dimensions/orientation/metadata/profile are deterministic and validated | Pass; 237x320, bottom-up, 1-bit `BI_RGB`, 40-byte DIB, offset 62 |
| Existing Phase 13–15 evidence boundary is preserved | Pass; this pre-capture review made no native BMP wrapper or persistence claim; Capture 01 later verifies the exact 16-byte wrapper and payload persistence |
| Complete pre/post backups, Manager snapshots, USB log, timestamps, and preservation manifest are required | Pass; runbook and role layout |
| Three required timestamps are unambiguous and separately mapped | Pass; idle, immediately pre-send, completed/packet-idle |
| Fresh pre-operation backup and target absence are mandatory | Pass |
| Correct device row is required | Pass; `USB\\Vid_054c&Pid_001e`, not a root hub |
| Abort conditions cover wrong target, failed/ambiguous Manager result, incomplete evidence, and unrelated changes | Pass |
| Raw evidence and private/original content remain outside Git | Pass; no external capture created by this task |
| Normal CLI/GUI package transfer remains untouched and disabled | Pass |
| Modern device-changing operation is excluded | Pass |
| Focused fixture and existing typed-media tests pass | Pass; 19 tests |
| `git diff --check` after review changes | Pass |

## Review observations and correction loop

The initial host test used an incorrect expected byte for a bottom-up BMP row.
The test was corrected to assert the generated top/bottom border and interior
diagonal/cross sentinels at their actual bottom-up offsets. The generated BMP
source remained unchanged; its hash and all manifest bindings still match.

The existing offline model deliberately reports only a logical typed package
and a lower-bound growth estimate. It does not invent a BMP native wrapper,
native metadata, candidate, transaction, capacity result, or device outcome.
The procedure carries those as explicit native evidence questions. No native
claim is inferred from fake transport, deterministic reconstruction, or the
previous four-TXT smoke.

## R3 decision

The exact fixture, preservation requirements, three-timestamp procedure,
abort policy, and evidence classifications were sufficient for one isolated
legacy capture. This remains the historical pre-capture readiness checkpoint;
the later Capture 01 result and owner confirmation supersede its pending
status for the exact native evidence scope. The final focused and complete
host suite passes 529 tests with three intentional evidence-dependent skips.
P16-001 is therefore recorded as **COMPLETE** for the exact
`IC_P16_MIXED_20260830_01` TXT/BMP/TXT package only.

Before the Manager changes the device, the owner must supply the exact
operation-specific phrase:

```text
APPROVE P16-001 LEGACY MIXED TXT BMP CAPTURE 01
```

After approval, the human operator must follow the runbook and preserve the
complete external evidence set. If capture is incomplete, Manager cannot
create/send the BMP package, or any required comparison is unexplained, the
result is `BLOCKED_BY_EXTERNAL_EVIDENCE` or `ESCALATION_REQUIRED` as defined
by the protocol. No modern implementation or modern transaction may be
prepared from this readiness record alone.
