# P17-015 — Independent R3 review

Date: 2026-09-01
Review scope: immutable operation bundle and real live-entrypoint rehearsal
Disposition: **PASS — READY_FOR_HARDWARE_TEST host-only**

## Independent checks

The review inspected the PR #21 diff, the P17-005 live entrypoint, the strict
P17-013 sealed-report loader, the P17-008 raw-state identity boundary, the
P17-010 corrected freshness boundary, the independent post-operation verifier,
and the new bundle module/tests. The review treated the P17-010 clock,
P17-012 schema, and P17-014 wrong-baseline events as separate historical
fail-closed findings.

The following controls pass:

- the public live entrypoint has one operation input: the immutable bundle;
  catalog, selected item, baseline, template, capacity, candidate,
  transaction, report, and output destinations are not independently passed;
- the bundle self-hash and every artifact hash are checked before runtime
  callbacks, and the sealed report's baseline path/hash must match the bundle;
- the strict P17-013 schema remains authoritative, including the nested
  `authorization.candidate_transaction_sha256` binding;
- the bundle package-child list, catalog/package manifest, source/prepared
  files, template, capacity response, raw-state identity, candidate,
  authorization, timestamp policy, expected post-state, and both seals are
  cross-checked rather than trusted from one copied field;
- changed, substituted, malformed, duplicated, or contradictory bundle
  members fail before detection/capture/sender access; the P17-014 report plus
  P17-012-style baseline substitution is directly covered;
- output paths are distinct, JSON evidence output is named as a file, and
  existing destinations are rejected before the run;
- the existing ordered device gates, corrected post-finalization freshness,
  capacity, fixed/display-history, target absence, cancellation, one-shot,
  explicit `0x0000`, no-retry, post-backup, and independent read-back controls
  remain in the execution path;
- the real production entrypoint has two nominal fake-only rehearsals with
  one send and verified read-back, plus a sender-boundary rehearsal with zero
  sends; no production GUI/CLI import path reaches this adapter; and
- raw evidence and candidate/transaction bytes remain outside Git.

## Review findings and correction loop

The initial implementation review identified three host-only issues before
the final pass: the resolver needed an explicit expected-post-state comparison;
the evidence destination needed a file-shape guard; and a substitution test
needed to use canonical resolved paths when rebuilding its self-hash. These
were corrected in the bounded correction round. The focused suite was then
rerun, including all prior P17-005/P17-013 failure classes and the new bundle
matrix.

No material safety finding remains. The bundle is not a general artifact
format, does not weaken raw-byte/state verification, and does not add a
normal product transfer interface.

Final validation passed: the focused production-entrypoint suite ran 29 tests,
the focused P17-003/P17-005/state/read-back regression set ran 51 tests, and
the complete portable suite ran 618 tests with 3 intentional evidence-dependent
skips. Source compilation without bytecode writes, `git diff --check`, the
excluded-evidence/history audit, and the normal CLI/GUI import-isolation check
also passed. The added child-binding matrix covers every ordered-child field;
loader-level checks cover fixed identity, timestamp policy, endpoint, child
order, duplicate JSON, and fixed safety projection.

## Evidence classifications

- **Verified:** host code/test review and fake-boundary results listed above;
  no sender or hardware boundary is reachable until bundle resolution and all
  existing gates pass.
- **Observed:** fake backend request counts and temporary evidence creation in
  the automated rehearsals.
- **Inferred:** a future exact live operation can use this boundary only with
  a new fresh preflight and new owner approval.
- **Unresolved:** real-device compatibility and all broader package,
  completion, recovery, and normal product-transfer behavior.

## Final review decision

**PASS.** P17-015 is **READY_FOR_HARDWARE_TEST** for the exact reviewed
Library package profile, host-only. No hardware access, approval phrase,
sender construction, or `0x101b` transmission is part of this task. A later
task must perform a new fresh read-only preflight and obtain new exact owner
approval.
