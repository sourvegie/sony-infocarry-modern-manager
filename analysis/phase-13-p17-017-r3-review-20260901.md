# P17-017 — Independent R3 review

Date: 2026-09-01
Risk: R3 — immutable authorization and one-shot live entrypoint
Disposition: **PASS — READY_FOR_HARDWARE_TEST (host-only)**

## Review boundary

This review covers the P17-017 correction that separates immutable operation
safety identity from per-attempt evidence outputs. The preserved P17-016
output-collision failure remains unchanged outside Git. No hardware, real
sender, approval phrase, or `0x101b` was used.

The reviewer examined the immutable bundle schema, live adapter, evidence
reservation and manifest validation, production-entrypoint fake rehearsals,
failure/replay tests, and the sanitized status, risk, roadmap, and P17-017
analysis records.

## Findings and correction loop

The first independent review did not pass. It identified three corrections:

1. A live failure after safety-state acquisition could replay because the
   one-shot claim was consumed too late.
2. The evidence namespace accepted repository and symlink roots.
3. The status record contained the malformed phrase “are resolver verifies.”

The corrected runner consumes the live claim immediately after exact approval
and safe pre-callback cancellation, before any device-state callback. Thus a
live detection, capacity, backup, candidate, or later safety failure expires
the approval and cannot be retried. Preflight-only mode and safe cancellation
before callbacks remain non-consuming. The namespace now must be an existing
absolute non-symlink directory outside the source repository, with one
exclusive direct-child attempt root. The status wording was corrected.

A second review found one audit inconsistency at the missing-backend boundary:
the consumed claim was reported as `approval_consumed=false`. The audit now
uses the actual consumed-claim value and the focused test asserts
`approval_consumed=true`. The final re-review found no remaining R3 findings.

## Validation

- Focused P17-017 adapter suite: **34 passed**.
- Complete portable suite: **623 passed, 3 intentional skips**.
- In-memory source compilation: passed.
- `git diff --check`: passed.
- Excluded-evidence/history audit: no current tracked raw backup, capture,
  candidate, transaction, or USB artifact; the pre-existing historical
  `analysis/private-remote-audit-2026-08-23.md` continues to document an old
  blocked raw-fixture history finding and is not new evidence.
- Independent R3 final re-review: **PASS**, with no remaining findings.

## Verified host boundary

The bundle self-hash excludes concrete attempt paths but retains every
safety-relevant artifact, state, package, candidate, transaction, capacity,
approval, one-shot, completion, and no-retry binding. A sealed bundle can run
preflight-only into one reserved external root and then one approved live fake
attempt into a distinct root without rebundling. The fake live path sends once,
accepts only explicit `0x0000`, captures post-operation evidence, and passes
independent read-back. Collision, namespace, tamper, drift, and replay cases
fail closed before sender construction or without retry as applicable.

Physical compatibility, native completion semantics, and any real-device
operation remain unresolved and out of scope. A later task must perform a new
fresh preflight and obtain new operation-specific approval.
