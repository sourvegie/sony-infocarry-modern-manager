# P15-001 — independent R3 offline review

Date: 2026-08-30
Disposition: **PASS WITH BOUNDED UNRESOLVED NATIVE COMPLETION — READY_FOR_HARDWARE_TEST**

This review is an independent host-side pass over the preserved Capture 01
hashes, the sanitized package, the generated candidate, authorization,
transaction, and fake-only read-back artifacts. It does not authorize or
perform hardware I/O.

## Review checks

| Check | Result |
| --- | --- |
| Exact four-source package, hashes, CP932/CRLF boundary, and order | Pass |
| Raw evidence and raw timestamp preservation | Pass; raw timestamp files unchanged |
| Pre/post backup completeness, identity, hashes, and target delta | Pass |
| Native record offsets, parent marker, table width, child order, wrappers, payloads, alignment | Pass |
| Native range declaration and range-to-post-blob equality | Pass |
| Capacity and five fixed-state bindings | Pass |
| Unrelated records, payloads, and objects | Pass; legacy shared timestamp rewrite retained as observed |
| Owner timestamp mapping and normal-return observation | Pass; recorded as observed, not numeric completion |
| Native completion decoding | Unresolved; no contradictory numeric result was found |
| Modern timestamp policy | Pass; existing timestamps preserved and one explicit new-record value used |
| Candidate/authorization/transaction binding | Pass |
| Fake-only workflow and independent read-back | Pass; one fake call, simulated `0x0000`, no retry |
| Modern hardware transaction | Not performed |

## Correction loop

1. Review found that the fake workflow always declared a BMP template, causing
   a valid TXT-only package reconstruction to fail. The workflow was corrected
   to include required templates and only validated optional templates present
   in the supplied template; a new TXT-only regression test passes.
2. Review found a derived native-versus-modern report used a mask that counted
   timestamps as non-timestamp metadata. The report was corrected. It now
   records 313 shared timestamp differences plus one native child-4 difference
   and zero non-timestamp metadata differences.
3. Re-review passes with the native numeric request-4 word explicitly
   unresolved. The owner normal-return observation and post-backup persistence
   are retained as observed/verified evidence rather than upgraded to a
   decoded completion.

## R3 decision

The evidence supports only the exact flat one-folder/four-ordered-TXT shape
and the approved modern timestamp policy. The dossier may stop at
`READY_FOR_HARDWARE_TEST`. It must not be treated as a live approval, a claim
for arbitrary packages, or proof of physical modern multi-child compatibility.
