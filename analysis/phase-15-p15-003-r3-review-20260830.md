# P15-003 — independent R3 review

Date: 2026-08-30  
Disposition: **PASS — COMPLETE for the exact constrained smoke**

This review independently checks the sanitized implementation and the
owner-approved P15-003 evidence synthesis. It does not authorize another
device operation.

## Review checks

| Check | Result |
| --- | --- |
| PR #2 commit `e7c07e9` merged into canonical `main` before task start | Pass; merge commit `02dc39e` |
| P15-003 used a task branch based on clean canonical `main` | Pass |
| Capture 01 and `_01` package preserved | Pass; 119/119 external manifest entries match |
| New `_02` destination distinct and absent from fresh baseline | Pass |
| Exact four source files, order, CP932-safe preparation, and hashes | Pass |
| Fresh complete before backup and `0x0019` capacity revalidation | Pass |
| Candidate, transaction, capacity, fixed-state, device, and preflight seal bindings | Pass |
| Exact approval and confirmation supplied in this task | Pass |
| At most one modern transaction | Pass; sender calls = 1 |
| Completion handling | Pass; explicit `0x0000` only |
| Post-operation complete backup | Pass; eight objects, complete manifest |
| Independent post read-back | Pass; candidate blob and exact five-path delta |
| Ordered children and TXT wrappers | Pass; offsets `0x400`–`0x4c0`, 32-byte wrappers |
| Existing timestamps and fixed state preserved | Pass |
| Unrelated records, payloads, and objects preserved | Pass |
| Automatic retry or corrective write | Pass; absent |
| Normal CLI/GUI multi-TXT package transfer exposure | Pass; remains disabled (the separately guarded existing-TXT replacement path is unchanged) |
| Cross-process preflight seal stability correction | Pass; focused regression added |

## Correction loop

The physical run exposed a host-only seal defect before execution: the seal
included `VerifiedBackup.verified_at_utc`, which necessarily changes when the
same complete backup is independently re-hashed later. No USB session had
opened at that failed reload. The approved operation then used the unchanged
raw archive, verified all stable baseline bindings, and retained the original
sealed observation value. The implementation correction removes only this
volatile observation from the seal input and preserves it in ordinary audit
output. The focused live-smoke suite passes with a regression test proving
that re-verification observation time does not invalidate the seal.

No candidate, persistent format, public interface, or normal transfer scope
was broadened by the correction.

The final isolation audit found no multi-TXT runner or package-candidate
reference in the normal CLI/GUI surfaces. Existing constrained replacement
imports remain present as their previously proven, separately guarded path;
they do not expose this multi-TXT runner or package transfer.

## R3 decision

The exact P15-003 modern four-TXT smoke is verified and may be marked
`COMPLETE` for its constrained scope. The result is not evidence for BMP,
nested, batch, arbitrary, delete, restore, synchronization, or interrupted
write behavior. Further device changes require a new operation-specific task
and owner approval.
