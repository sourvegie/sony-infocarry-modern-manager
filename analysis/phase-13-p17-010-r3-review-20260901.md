# P17-010 — Independent R3 review

Date: 2026-09-01  
Review scope: exact P17 Library package read-only preflight and final approval boundary  
Disposition: **PASS — corrections resolved and re-reviewed**

This record is the independent R3 review boundary for P17-010. The review was
performed after the implementation dossier and external evidence were
available and before the final task disposition.

## Required review checks

- Confirm PR #16 was merged without history rewriting at
  `98ae3e14b36073f5bb41c30c4ae4ee6a85c9b390`, and the task branch started
  from clean canonical `main`.
- Confirm the external evidence root is new, non-overwriting, outside Git,
  and its v3 manifest replays with zero mismatches; v1 and v2 remain preserved
  unchanged.
- Confirm the physical sequence was limited to one expected Sony `054c:001e`
  detection, one native `0x0019` query, and one complete eight-object backup;
  confirm no sender, backend write, or `0x101b` occurred.
- Confirm the fresh backup passed complete integrity verification before
  `BackupStateIdentity` derivation and that raw identity equality with P17-009
  is reported separately from retained provenance differences.
- Confirm `IC_P17_LIBRARY_20260831_03` is absent and the exact P17-004
  catalog/package/manifest/ordered child/template bindings are revalidated.
- Confirm candidate and transaction hashes are exactly the P17-009-approved
  values, while the new outer seal is allowed to differ because fresh backup
  provenance is bound.
- Confirm the six `0x001b` references, reviewed rebasing policy, all-zero
  `0x001c`–`0x001f` state, timestamp policy, capacity margin, additive path
  delta, and preservation rules remain exact and constrained.
- Confirm the later approval phrases are explicit, exact, unconsumed, and
  not inferred from this task.
- Confirm the isolated adapter is not reachable from normal GUI/CLI imports,
  constructs no sender during preflight, allows at most one later send, accepts
  only integer `0x0000`, never retries, and treats post-start ambiguity as
  terminal/indeterminate.
- Confirm the dossier classifies verified, observed, inferred, and unresolved
  facts without broadening physical compatibility claims.

## Findings and correction disposition

1. **P2 — capacity margin terminology.** The first custom sealed projection
   exposed the adapter's `capacity - baseline` allowance as an unlabeled
   `capacity_margin_bytes` field. The original report remains preserved. A
   non-overwriting corrected projection now labels the 1,070,472-byte baseline
   allowance and the 1,054,436-byte post-candidate headroom explicitly. The
   adapter seal, candidate, transaction, and raw evidence were not changed.
   **Resolved.**
2. **P3 — branch-cleanliness evidence.** Git verifies the task branch ancestry
   from the merged `main`, but cannot reconstruct the transient clean-tree
   state at branch creation. The claim is scoped as an observation recorded
   in the external branch-boundary audit, not as a historical Git proof; the
   branch is clean at review time. **Resolved by scoping; no safety gate was
   weakened.**

## Re-review result

The corrected report, v3 external manifest, and current repository state were
rechecked by a fresh independent reviewer. The complete v3 manifest replays
with zero mismatches; the raw
backup remains complete and raw-state equal to P17-009 while provenance
differences remain visible; the target remains absent; candidate and
transaction hashes remain exactly the approved P17-009 values; the supported
fixed/display-history policy and capacity fit remain unchanged; and sender
calls, backend writes, USB transmissions, and device changes remain zero.
The exact later phrases are bound but unconsumed. The isolated adapter remains
outside normal GUI/CLI imports and retains one-shot/no-retry behavior.

**R3 review passes. P17-010 may report `READY_FOR_HARDWARE_TEST` and must
stop before any approval phrase is consumed or any sender is constructed.**
