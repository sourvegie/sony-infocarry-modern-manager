# P16-003B — independent R3 review

Date: 2026-08-31
Risk: **R3 — device/safety critical**
Review boundary: one approved modern flat TXT/BMP/TXT operation only.

## Review scope

The independent review examined the P16-003B sanitized result, the merged
P16-002/P16-003A runner behavior, the sealed preflight report, the external
preservation manifest, the complete post-operation backup, the candidate and
transaction hashes, and the independent read-back result. Raw evidence was
kept outside Git and no second hardware operation was performed.

## Required checks

| Check | Result | Classification |
| --- | --- | --- |
| Exact scope | One new root folder with TXT → BMP → TXT children at `_02`; no nesting, batch, delete, or GUI/CLI exposure | verified |
| Owner authorization | Both exact phrases were supplied after the fresh sealed preflight | observed |
| Fresh preflight | Sony `054c:001e`, fresh `0x0019`, complete 8-object backup, target absence, exact seal | verified |
| Candidate binding | Source hashes/order, template, capacity, fixed state, semantic rebase, candidate, transaction, and timestamp policy revalidated | verified |
| One-shot boundary | Exactly one `0x101b` sender call; no automatic retry | verified |
| Completion | Explicit integer `0x0000` accepted; no ambiguous/nonzero completion | observed |
| Post backup | Complete 8-object backup; post dynamic blob equals candidate SHA-256 | verified |
| Mixed package | Exact folder, marker/children, order, payloads, TXT/BMP prefixes, and post-state are verified | verified |
| Display history | Three references rebase by exact `0x140` and resolve to the same `_01` paths/records | verified |
| Existing state | `_01` read flags `0x20`, existing payloads/timestamps, and `0x001c`–`0x001f` zero state preserved | verified |
| Unrelated preservation | Independent verifier reports unchanged unrelated records, payloads, and objects | verified |
| Evidence preservation | Versioned external manifest covers 65 entries with zero mismatches | verified |
| Safety boundary | No retry, corrective write, broader operation, or second hardware access | verified |

## Review findings and resolution

The initial preflight invocation sampled its freshness reference before the
read-only backup completed. The runner correctly rejected that reference as a
future-timestamp condition. The complete captured archive remained intact and
was not recaptured; a non-overwriting copy was verified with the runner's
normal current-time freshness behavior, and the exact candidate seal matched
the reported preflight seal. This is documented as an observed host procedure
correction, not normalized away.

No material discrepancy was found between the sealed candidate and the
post-operation backup. The post dynamic blob is byte-identical to the
candidate, while the fixed-state comparison independently verifies the exact
semantic `0x001b` rebase and all-zero remaining fixed-state responses.

## Validation record

- Exact runner execution: one sender call, explicit integer `0x0000`.
- Independent post read-back: passed for the exact constrained package.
- Post backup: 8 objects, 394 records, 331 reachable paths.
- Versioned external preservation manifest: 65 entries verified.
- Source package SHA-256 manifest: passed.
- Offline candidate/transaction hashes: matched the sealed preflight.
- No repository raw-evidence or private-device files were added.

## Evidence classification

### Verified

The exact P16-003B package, source hashes/order, native template/prefixes,
capacity fit, candidate, transaction, semantic display-history rebase, one-shot
runner, explicit completion, complete post backup, and independent read-back
all agree. Existing `_01` content and read-state flags remain preserved, and
the four fixed-state objects `0x001c`–`0x001f` remain all-zero.

### Observed

The owner supplied the exact operation approval/confirmation phrases. The
physical device returned the explicitly accepted `0x0000` completion, and the
post-operation archive was captured from the device after the one transaction.

### Inferred

The exact tested modern mixed TXT/BMP/TXT operation is physically compatible
with this device state. This does not generalize to arbitrary mixed packages,
nested packages, batch actions, or recovery behavior.

### Unresolved

Native numeric completion decoding, operation-specific capacity semantics,
physical interrupted-write recovery, and broader state/package compatibility
remain unresolved.

## Final disposition

**PASS — COMPLETE**, limited to the exact approved P16-003B flat TXT/BMP/TXT
operation. Normal GUI/CLI transfer remains disabled. No further hardware
operation is authorized by this result; a future write requires a new task,
fresh preflight, and separate exact owner approval.
