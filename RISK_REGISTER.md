# Risk Register

Date: 2026-09-22

This file lists open material risks only. Closed findings and chronological
evidence remain in `analysis/`, including the archived register through
P18-001A.

| ID | Risk | Level | Control / current boundary | Next action |
| --- | --- | --- | --- | --- |
| R2-18-01 | Capability-profile, hierarchy persistence, ordering, CP932/path limits, and Library drift could admit an unsupported shape. | R2 | P18-006 table-driven A–G matrix covers non-exact counts/order/types, package/content/binding drift, backup/capacity/conflict changes, and hierarchy/grouping substitution; deterministic hashes and ordering remain fail-closed; no nested candidate or live enablement. | Retain the exact host/offline boundary and add no live nested path; future capability changes require a new reviewed task. |
| R2-18-02 | Cross-platform packaging, font/licensing, hermetic-test, and Windows CI gaps could make the desktop workflow unreliable. | R2 | Keep conversion core portable; record environment assumptions; do not call visual/macOS checks universal. | Resolve licensing and portable CI gaps before a device-changing GUI milestone. |
| R3-18-03 | A future path could accidentally reach authorization, candidate construction, sender, or live-success decisions outside the reviewed boundary. | R3 | P18-006 proves A–O fail-closed reachability and exact reviewed V15 profile matching. P18-008 replaces the process-local claim with an injected SQLite authority keyed by the preflight seal, actual restart/crash/race coverage, and a durable sender-start marker; terminal and diagnostic marker resolution are proof-bound and fail closed. P18-020 routes existing-TXT replacement through the same neutral application-wide persistent owner. P18-039 carries bounded generalized flat children through the same canonical planner, candidate, readiness, authorization, operation-bundle, marker, lock, completion, post-backup, and independent-readback seams; invalid structures stop before those boundaries and the generalized profile remains non-live-proven. | Keep one canonical coordinator/owner, require exact-head CI and independent review, and keep the generalized profile out of physical execution until a separate owner-authorized validation procedure passes. |
| R3-18-04 | Ambiguous completion or interruption has no proven physical recovery/rollback. | R3 | No automatic retry; explicit integer `0x0000` only; malformed/boolean post-start completion is indeterminate; persistent installation-wide lock; sender-start marker promotion after crash; read-only diagnostic backup, original incident/attempt binding, and documented recovery decision required. | Keep restore/recovery and interruption experiments unavailable; physical recovery/rollback remains unproven and must be handled by a separately approved hardware task. |
| R2-18-05 | Legacy Manager semantics are incomplete for the broader capability envelope. | R2 | P18-007 now has an offline Oracle boundary at the preserved host-to-device `0x101b` command payload immediately before driver/USB submission, plus reusable raw/normalized differential tooling. Corpus A–C covers single TXT, four ordered TXT children, and the TXT/BMP/TXT product shape without importing legacy binaries or enabling live reachability. P15 normalizes only independently established timestamp/checksum differences; P13 retains an unexplained four-byte allocation/offset discrepancy; whole-blob P16 comparison is not comparable because its modern baseline/target identity differs, although the typed package projection matches. | Keep the P13 alignment discrepancy out of R3 production code and route any repair to a separately reviewed task; obtain a safe external final-buffer observation only if a same-baseline P16 whole-blob comparison or hierarchy evidence becomes necessary. |
| R2-18-06 | VNW-V10 may differ from VNW-V15 in USB identity, protocol, storage/format, capacity, display, or write behavior. | R2 | VNW-V10 is `UNCHARACTERIZED / READ-ONLY DISCOVERY REQUIRED`; no V15 constants, candidate, authorization, or write capability are inherited. | Conduct separately reviewed read-only V10 characterization before defining any capability. |
| R2-18-07 | No stable physical-unit identifier is proven, so a per-unit recovery lock would create false precision. | R2 | Use one installation-wide persistent lock across models/sessions; deliberately over-block all writes after ambiguity; clear only with original incident/attempt, complete diagnostic backup, and documented recovery decision. | Keep lock global until a future reviewed identity fact changes the contract. |
| R2-18-08 | Users may mistake a host device-tree preview or stale capacity figure for transfer eligibility. | R2 | Label the nested profile `host_offline_draft_not_live_enabled`; show all four capacity quantities as Not evaluated without fresh verified evidence; expose no nested send action. | Retain explicit preview-only language in GUI/docs and cover capability mismatch in focused tests/review. |
| R3-18-09 | The normal ttk Library path could bypass the reviewed operation identity or canonical one-shot lifecycle when live integration becomes reachable. | R3 | P18-017 uses one neutral product facade, typed fresh VNW-V15 binding, hash-bound operation ID/target/package bundle, final coordinator revalidation, durable claim-before-marker ordering, no retry, and the existing post-backup/independent-verifier closure. P18-020 makes existing-TXT replacement use the same persistent owner. P18-039 routes ordinary flat folders through the same responsive controller and simple confirmation path; generalized host admission carries dynamic ordered children but does not add a sender, alternate safety owner, or physical capability. Unsupported structures fail before authorization/claim/marker/sender; launch remains inert; absent/mismatched template or safety state fails closed. | Keep VNW-V10, nesting, deletion, overwrite/merge, and generalized physical execution blocked; complete exact-head package/CI and independent review before readiness, then require separate operation-specific owner approval/procedure for hardware. |
| R2-18-10 | Packaged Windows environments may differ in Tk behavior, libusb deployment, driver/access setup, or x64/ARM execution from the validated development environments. | R2 | Treat Windows support as an evidence programme, keep the conversion core portable, and do not relax the selected Tk/runtime requirement from assumptions alone. | Build and clean-install-test the actual package on declared Windows x64 and ARM environments, including Tk and libusb, before advertising compatibility or changing the runtime requirement. |
| R2-18-11 | Untrusted EPUB ZIP/XML/HTML could trigger path traversal, decompression/resource exhaustion, external entity or remote-resource access, script execution, or silent content loss. | R2 | `ContentWorkspace` uses bounded ZIP reads, normalized duplicate/path rejection, compression and size limits, entity rejection, no extraction/network/script execution, strict OPF/spine checks, and explicit unsupported-feature classifications; prepared output remains host-only. | Keep the parser subset bounded, add adversarial fixtures, and obtain independent review before treating EPUB preparation as a supported product capability. |

P18-020 resolves the known application-wide write-safety bypass; R3-18-03
continues only as a regression/new-path risk. SQLite and JSON storage
consolidation is deliberately deferred until migration and crash-consistency
behavior are proven, while one application-wide safety owner remains mandatory.

P18-037's ordinary-folder adapter is limited to transiently materializing the
existing exact three-/four-leaf package contracts after generic host planning,
source revalidation, and unchanged-byte preparation. The in-progress packaged
runtime wiring remains bound to the canonical readiness/preflight machinery
and shared safety owner; it adds no physical capability envelope, alternate
candidate, sender, or safety owner. The external reviewed template is hash
checked and configuration fails closed. Exact-head tests, package smokes/CI,
and independent review are pending; see the dated P18-037 analysis record.

P18-001A responsive usability is closed for the owner-observed bounded sizes;
that human observation does not establish hardware or other display-environment
behavior. Historical P15–P18 risks and dispositions are preserved in the
archived register and their analysis records.
