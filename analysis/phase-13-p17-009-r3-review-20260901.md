# P17-009 — independent R3 review

Date: 2026-09-01
Reviewed branch: `task/P17-009-corrected-raw-state-preflight`
Baseline: canonical `main` after PR #15 at `cd086b5e292f7d0ff6f4eefe8a2a5350864f9ad9`
Disposition: **PASS — READY_FOR_HARDWARE_TEST at the host-only boundary**

## Review scope

The independent review covered the P17-009 fresh read-only preflight dossier,
the P17-008 `BackupStateIdentity` implementation, the explicit operation
phrase policy, the isolated P17-005 adapter, the one-shot/no-retry boundary,
the fake-host failure tests, and the current status/risk/roadmap updates.
The reviewer did not edit files, access USB or hardware, invoke a sender, or
modify raw or external evidence.

## Initial findings and finite correction loop

The first independent R3 review found one material issue and one evidence-index
hygiene issue:

1. **High — one-shot enforcement was invocation-local.** The local
   `sender_calls` counter reset on each execution call, allowing the same
   sealed preflight and phrases to reach a second invocation after a
   post-start failure.
2. **Low — preservation-manifest index contradiction.** The immutable v1
   manifest named `02-sealed-preflight-0002/sealed-preflight.json` as both
   authoritative and superseded.

The correction added a thread-safe process-local single-use claim keyed by the
sealed preflight SHA-256. It is consumed immediately before sender
construction, so a repeated call or same-seal copied preflight fails closed;
pre-send cancellation and earlier failed gates do not consume it. A focused
regression test proves second-execution refusal.

The raw evidence and v1 manifest were not modified. A non-overwriting v2
manifest preserves v1 as evidence, corrects the superseded list, and covers
68 entries. The v2 replay reports zero mismatches; v1 remains unchanged.

A second independent reviewer then identified a **P2** boundary issue: the
generic explicit confirmation policy could accept the historical fixed/P17-007
confirmation outside the live adapter. The correction now rejects that
historical phrase in both the authorization constructor and authorization
factory under explicit policy, with focused regression coverage. The live
adapter continues to reject both expired P17-007 phrase fields.

## Final independent re-review

The corrected working tree was independently re-reviewed. Final reviewers
confirmed:

- complete/integrity backup verification precedes raw-state identity derivation;
- device/protocol/object-set/role/command/filename/length/raw-hash,
  dynamic-blob, and fixed-state identity differences remain fail-closed;
- acquisition path, manifest hash, and timestamps remain preserved and are
  reported as provenance rather than normalized into state;
- exact package, catalog, source, template, target, capacity, display-history,
  candidate, transaction, seal, and expected-post-state bindings remain exact;
- explicit P17-009 phrases are sealed, and the expired P17-007 phrases are
  rejected centrally and by the live adapter;
- the single-use claim is thread-safe, keyed by the sealed operation, and
  consumed before sender construction; no second execution can send through
  that sealed operation;
- cancellation before transmission remains safe, while post-start timeout,
  disconnect, missing/malformed/nonzero completion, and read-back failures
  remain terminal with no retry; and
- normal GUI/CLI isolation and fake-host-only testing remain intact.

The independent final disposition is **PASS**. It confirms the host-only
`READY_FOR_HARDWARE_TEST` boundary and does not authorize or consume either
operation phrase.

## Validation checkpoint

- Focused R3 adapter/gate/bridge/identity/workflow/read-back/write tests:
  **83 passed**.
- Complete portable suite: **599 passed, 3 intentional
  evidence-dependent skips**.
- `git diff --check`: passed.
- Source/test compilation: passed.
- P17-009 v2 external preservation replay: **68 entries, zero mismatches**;
  v1 remains preserved and unchanged.
- Excluded-evidence/history audit: no tracked or reachable forbidden raw
  evidence paths/extensions.
- No USB, hardware, sender, backend write, or external raw-evidence mutation
  occurred during review or host validation.

P17-009 is **READY_FOR_HARDWARE_TEST** only at the host-only boundary. Any
later live task requires a new fresh preflight and new exact owner approval;
this task must stop before `0x101b`.
