# P17-013 — Independent R3 review

Date: 2026-09-01
Reviewer: independent R3 review pass, separate from implementation
Scope: strict P17-012 sealed-report loader correction
Disposition: **PASS — READY_FOR_HARDWARE_TEST only after merge**

## Review basis

The review inspected the P17-013 source/test diff, the canonical producer and
core bridge, `PreparedLibraryPackageLivePreflight.verify_seal()`, the P17-012
sanitized dossier/review, and the preserved sealed report at
`/Users/stardust/Projects/InfoCarry-Evidence/phase-17-p17-012-library-package-live-preflight-20260901-01/03-sealed-preflight-0001/sealed-preflight.json`.
The preserved P17-012 attempt-03 failure file remains outside Git and was not
modified; its current SHA-256 is
`5a25145b156195a56347a1af10c7902aa85eb1e7769328af0a1dcee42353da4b`.

This review was read-only with respect to evidence and hardware. No device
was detected, no capacity or backup was acquired, no sender was constructed,
no approval was consumed, and no `0x101b` was issued.

## Controls confirmed

- The loader requires the exact P17-012 top-level key set. A missing nested
  transaction field, top-level-only alias, contradictory alias, unexpected
  field, malformed JSON, or duplicate JSON key fails at `preflight_load`.
- The exact preserved P17-012 report has no top-level `transaction_sha256`;
  its `authorization.candidate_transaction_sha256` is the reviewed
  `1d1adc02cee8b856e2e8281ff623e84e681ec893793bebacb247a82c788749d5`.
- All execution-state scalars are type-checked and compared to the reviewed
  read-only/no-write state. The producer and report correctly distinguish
  read-only hardware access from USB transmission and device change.
- The loader reconstructs the candidate through the existing Library bridge
  from independently supplied verified inputs. It does not treat report
  candidate or transaction data as byte authority.
- The nested transaction digest is compared with the independently rebuilt
  transaction before the reconstructed core and outer seals are verified.
  Full nested authorization, candidate, backup, capacity, and state bindings
  remain covered by the existing verifier.
- The preserved P17-012 report replays offline with outer seal
  `aeb656176aa9163d904f1f8fcbf9c588ef7a33be35dfcc2e64efa3b8235829a8`,
  candidate
  `a5e9ca7f429a6f75583c1c5701bb669ed2b7f79e068dda69c63bb06300174761`, and
  transaction `1d1adc02cee8b856e2e8281ff623e84e681ec893793bebacb247a82c788749d5`.
- Focused tests cover valid loading, missing/alias/contradictory bindings,
  malformed and mismatched hashes, duplicate keys, and zero callback/sender
  activity on every loader failure. Existing one-shot, no-retry,
  pre-send-failure, post-start-indeterminate, read-back, and GUI/CLI
  isolation suites remain passing.
- Tracked-file and history inspection found no raw evidence, candidate bytes,
  transaction ranges, or excluded binary artifacts. Raw evidence remains
  outside Git.

## Findings and correction loop

No material R3 finding remains. The implementation correction and the
producer's read-only access flags were rechecked after the focused test pass;
the complete suite was rerun afterward. This is a host-only correction and
does not authorize or request the expired P17-012 phrases.

## Validation

- focused P17 live-adapter tests: 20 passed;
- complete portable suite: 609 passed, 3 intentional evidence-dependent
  skips;
- changed-file compilation: passed;
- `git diff --check`: passed; and
- excluded-evidence/history audit: passed with no tracked raw artifacts.

## Conclusion

P17-013 passes the independent R3 review for the exact sealed-schema loader
correction. It may be reported **READY_FOR_HARDWARE_TEST** only after the
task branch is committed, CI-passing, reviewed, and merged. A later task must
perform a new fresh read-only preflight and obtain new exact operation
approval. No hardware access, sender construction, retry, or `0x101b`
transmission is permitted in P17-013.
